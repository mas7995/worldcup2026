# worldcupapi.com API key — original live score sync (now inactive)
WORLDCUP_API_KEY=

# api-football.com key — for knockout bracket sync
# Free tier: 100 req/day at https://api-football.com
API_FOOTBALL_KEY=

# Admin panel PIN (default: 2026)
ADMIN_PIN=2026

# Server port (default: 3001)
PORT=3001

# Path to the SQLite database file.
# LOCAL: leave blank — defaults to server/worldcup.db
# CLOUD (Railway/Fly.io/Render): point to a persistent volume so data
# survives re-deploys. e.g. DB_PATH=/data/worldcup.db
DB_PATH=
